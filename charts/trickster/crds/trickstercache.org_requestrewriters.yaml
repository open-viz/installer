apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.10.0
  creationTimestamp: null
  name: requestrewriters.trickstercache.org
spec:
  group: trickstercache.org
  names:
    kind: RequestRewriter
    listKind: RequestRewriterList
    plural: requestrewriters
    singular: requestrewriter
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: RequestRewriter is the Schema for the requestrewriters API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: RequestRewriterSpec defines the desired state of RequestRewriter
            properties:
              instructions:
                description: RewriteList is a list of Rewrite Instructions
                items:
                  items:
                    type: string
                  type: array
                type: array
            type: object
          status:
            description: RequestRewriterStatus defines the observed state of RequestRewriter
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
