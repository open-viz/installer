{{- if eq "true" ( include "cleaner.generate" . ) }}
apiVersion: batch/v1
kind: Job
metadata:
  name: {{ include "grafana-operator.fullname" . }}-cleaner
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "grafana-operator.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": pre-delete
    "helm.sh/hook-delete-policy": hook-succeeded,hook-failed
spec:
  backoffLimit: 3
  activeDeadlineSeconds: 120
  template:
    spec:
      {{- include "appscode.imagePullSecrets" . | nindent 6 }}
      serviceAccountName: {{ include "grafana-operator.serviceAccountName" . }}
      containers:
      - name: kubectl
        image: {{ include "cleaner.registry" . }}/{{ .Values.cleaner.repository }}:{{ .Values.cleaner.tag }}
        command:
          - sh
          - -c
          - "sleep 2; \
          kubectl delete validatingwebhookconfigurations validators.openviz.dev || true; \
          kubectl delete mutatingwebhookconfiguration mutators.openviz.dev || true; \
          kubectl delete apiservice v1alpha1.mutators.openviz.dev || true; \
          kubectl delete apiservice v1alpha1.validators.openviz.dev || true;"
        imagePullPolicy: {{ .Values.imagePullPolicy }}
      restartPolicy: Never
{{ end }}
